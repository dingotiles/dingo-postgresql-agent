# Services/Processes running inside container

Using chaperone to manage processes. Why? One, we already had Python3 installed. Two, services can be described in separate files which is nice and modular.

Format for files is described at http://garywiz.github.io/chaperone/ref/config-service.html#service
