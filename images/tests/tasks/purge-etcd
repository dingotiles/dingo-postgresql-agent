#!/bin/bash

set -e
set -x

etcdctl --debug --endpoint "http://${ETCD_HOST_PORT}" ls --recursive /service/${PATRONI_SCOPE}
etcdctl --debug --endpoint "http://${ETCD_HOST_PORT}" rm --recursive /service/${PATRONI_SCOPE}
